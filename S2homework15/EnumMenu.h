#pragma once

enum MAIN_MENU {
	STUDENT = 0, TEACHER, EXIT
};

enum STUDENT_MENU {
	LOGIN = 0, REGISTER, GO_BACK
};

enum TEACHER_MENU {
	CREATE_TEST = 0, RETURN_BACK
};

enum YES_NO_MENU {
	YES = 0, NO
};

enum TestMenuOptions {
	START_TEST, EXIT_TEST 
};